akka {
  loglevel = "DEBUG"
  # stdout-loglevel = "DEBUG"
  log-dead-letters-during-shutdown = on
  actor {
    debug {
      receive = on
      autoreceive = on
      lifecycle = on
      fsm = off
    }
  }
}
